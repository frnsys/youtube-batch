Requires:
* ffmpeg
* youtube-dl (http://rg3.github.io/youtube-dl/)

To use, add YouTube urls to `clips.txt`.

Then run:

`$ python dl.py`

And then run:

`$ ./convert.sh`

You may need to:

`$ chmod +x convert.sh`
